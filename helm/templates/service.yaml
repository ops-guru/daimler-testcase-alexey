apiVersion: v1
kind: Service
metadata:
  name: {{ include "ag-challenge.fullname" . }}
  labels:
{{ include "ag-challenge.labels" . | indent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "ag-challenge.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
