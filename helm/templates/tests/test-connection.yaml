apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ag-challenge.fullname" . }}-test-connection"
  labels:
{{ include "ag-challenge.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "ag-challenge.fullname" . }}:{{ .Values.service.port }}/v1/ui']
  restartPolicy: Never
